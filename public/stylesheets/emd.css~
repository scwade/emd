/*div { border: 0 px solid red !important; }  */

body { background-color: #fff; color: #262262; }

body, p, ol, ul, td {
  font-family: verdana, tahoma, helvetica, sans-serif;
  font-size:   13px;
  line-height: 18px;
}

pre {
  background-color: #eee;
  background-color: #000;
  padding: 10px;
  font-size: 11px;
}

a { color: #444; }
a:visited { color: #666; }
a:hover { color: #fff; background-color:#888; }

.fieldWithErrors {
  padding: 2px;
  background-color: red;
  display: table;
}

#errorExplanation {
  width: 400px;
  border: 2px solid red;
  padding: 7px;
  padding-bottom: 12px;
  margin-bottom: 20px;
  background-color: #f0f0f0;
}

#errorExplanation h2 {
  text-align: left;
  font-weight: bold;
  padding: 5px 5px 5px 15px;
  font-size: 12px;
  margin: -7px;
  background-color: #c00;
  color: #fff;
}

#errorExplanation p {
  color: #333;
  margin-bottom: 0;
  padding: 5px;
}

#errorExplanation ul li {
  font-size: 12px;
  list-style: square;
}

/* Styles for main page */
#banner {
  background: #C4D1EB;
  padding-top: 10px;
  padding-bottom: 10px;
  border-bottom: 2px solid;
  font: small-caps 40px/40px "Times New Roman", serif;
  color: #262262;
  text-align: center;
}
#banner img {
  float: left;
}
#columns {
  background: #C4D1EB;
}
#main {
  margin-left: 13em;
  padding-top: 4ex;
  padding-left: 2em;
  background: white;
}
#side {
  float: left;
  padding-top: 1em;
  padding-left: 1em;
  padding-bottom: 1em;
  width: 12em;ver
  background: #C4D1EB;
}
#side a {
  color: #7895BC;
  font-size: small;
}
#notice {
  border: 2px solid red;
  padding: 1em;
  margin-bottom: 2em;
  background-color: #ECF0F9 ;
  font: bold smaller verdana;
}

/* Styles for partial in the sidebar (aka my_record) */
#record, #record table {
  font-size: smaller;
  color:       white;
  height: 30px;
}
#record table {
  padding: 5px 5px 5px 15px;
  border-collapse:separate;
  border-spacing:5px 10px 0px 5px;
  border-top: 1px dotted #595 ;
  border-bottom: 1px dotted #595 ;
  margin-bottom: 5px;
}
thead {
  font: bold large verdana;
}

table .X3 body { border="4"}
table .X3 th { font: bold xx-small verdana } 
table .X3 td { cellspacing: 1em; padding: 1em; font-weight: bold xx-small verdana }
table .X3 tr.odd { background-color: #d8ffff }
table .X3 tr.evn { background-color: #ffdfff }

.record-title {
   font: bold medium verdana;
}
.record-name {
   font: 120% bold;
}
.item-price, .total-line {
   text-align: right;
}
.item-count, .total-line {
   text-align: right;
}
.total-line .total-cell {
   font-weight: bold;
   border-top: 1px solid #595 ;
   
/* An entry in the my_record list */
#my_record .entry {
  overflow: auto;
  margin-top: 1em;
  border-bottom: 1px dotted #77d ;
}
#my_record .title {
  font-size: 120%;
  font-family: sans-serif;
}
#my_record .entry img {
  width: 75px;
  float: left;
}
#my_record .entry h3 {
  margin-top: 0;
  margin-bottom: 2px;
  color: #227 ;
}
#my_record .entry p {
  margin-top: 0.5em;
  margin-bottom: 0.8em;
}
#my_record .entry .price-line {
  clear: both;
}
#my_record .entry .add-to-cart {
   position: relative;
}

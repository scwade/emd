<h1>patients index</h1>

<table class="X3"rules="none" border="1">
  <tr>
    <th>patient condition ID</th>
    <th>conditions ID</th>
    <th>blood type</th>
  </tr>

<% @patients.each do |patient| %>
  <tr>
    <td><%=h patient.patient_condition_id %></td>
    <td><%=h patient.condition_id %></td>
    <td><%=h patient.blood_type %></td>


    <td><%= link_to 'Show', patient %></td>
    <td><%= link_to 'Edit', edit_patient_path(patient) %></td>
    <td><%= link_to 'Destroy', patient, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New patient', new_patient_path %>

<h1>My Personal Pending Records</h1>
<div class="record-title">My Personal Pending Records </div>

<!--    <table id="record">  -->
    <table>
    <% for item in @record.items %> 
      <tr>
        <td><%= item.quantity %> &times; <%=h (item.name) %></td>         
        <td><%=h (item.name) %></td>
      </tr>
    <% end %>
<!--      <td class="total-cell"><%= @record.total_quantity %></td>  -->
  </table>
 
  <%= button_to "Clear Records", :action => :clear_record %>
<!-- since above line is broken -- run below -->  
  <div id="main">    
    <table>
      <% render(:partial => "record_item", :collection => @record.items) %>
      <tr class="total-line"> 
        <td colspan="2">Records to add </td>
        <td class="total-cell"><%= @record.total_quantity %></td>
      </tr>
    </table>

</div>  

